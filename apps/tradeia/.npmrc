# npm configuration for Vercel deployment
# This helps avoid the "Cannot read properties of null (reading 'matches')" error

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Don't save exact versions
save-exact=false

# Prefer online packages
prefer-online=true

# Skip audit during install to speed up deployment
audit=false
fund=false
